{
  "expo": {
    "name": "remote-host",
    "slug": "remote-host",
    "version": "0.1.0",
    "orientation": "default",
    "scheme": "remote-host",
    "userInterfaceStyle": "dark",
    "splash": {
      "resizeMode": "contain",
      "backgroundColor": "#0d0d0d"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.famgia.remotehost",
      "infoPlist": {
        "NSCameraUsageDescription": "Camera is used to scan the pairing QR code displayed on the host."
      }
    },
    "android": {
      "adaptiveIcon": {
        "backgroundColor": "#0d0d0d"
      },
      "package": "com.famgia.remotehost",
      "permissions": [
        "android.permission.CAMERA"
      ]
    },
    "web": {
      "bundler": "metro",
      "output": "static"
    },
    "plugins": [
      "expo-router",
      [
        "expo-camera",
        {
          "cameraPermission": "Allow remote-host to access your camera to scan the pairing QR code."
        }
      ],
      [
        "expo-image-picker",
        {
          "photosPermission": "Allow remote-host to access your photos so you can send an image to the terminal."
        }
      ],
      "expo-secure-store"
    ],
    "experiments": {
      "typedRoutes": true
    }
  }
}
